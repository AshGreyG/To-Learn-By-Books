# HEAD is an important pointer, it indicates the position of working
# directory. If we are working on some branch, the HEAD points to this
# branch. We can change HEAD manually to a branch or a commit.

echo "git checkout c4"